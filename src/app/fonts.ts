import { Bitter, Open_Sans } from 'next/font/google'

export const open = Open_Sans({ subsets: ['latin'], variable: '--font-open' })
export const bitter = Bitter({ subsets: ['latin'], variable: '--font-bitter' })
